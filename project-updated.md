# Project Updates

## [2026-04-06] - Image Update for AgentDock
- Moved `apinject.png` from project root to `public/assets/`.
- Updated `src/components/Projects.tsx` to use `apinject.png` for the **AgentDock** (ApInject) project instead of `p4.png`.
- Updated `src/components/Projects.tsx` to use `apinject.png` for the **AgentDock** (ApInject) project instead of `p4.png`.

## [2026-04-12] - README Overhaul

- Added status badges (Live, React, TypeScript, Vite, Tailwind, Deploy).
- Added full **Table of Contents**.
- Upgraded Features section to a table with emoji icons.
- Added **Architecture & Data Flow** section documenting Spotify token flow, GitHub GraphQL heatmap, and EmailJS contact form pipeline.
- Added **Keyboard Shortcuts** table (`Ctrl+K`, `Esc`, `↑↓`, `Enter`).
- Added `<details>` expandable steps for each API credential (Spotify, GitHub, EmailJS).
- Added **Available Scripts** table (`dev`, `build`, `preview`, `lint`, `clean`).
- Added **Performance Notes** section (WebP images, GSAP threshold, no polling, code-splitting).
- Added **Design Decisions** section explaining `HashRouter`, custom SVG heatmap, `cmdk`, EmailJS.
- Added **Roadmap** section with potential future improvements.
- Tech Stack table updated to include Radix UI, shadcn/ui, Express serverless, and version numbers.
